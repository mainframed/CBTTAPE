
Function: NULL

Syntax:   (NULL arg1)

 arg1 (required,evaluated) - any LISP object.

Returns:

 T if arg1 is NIL, otherwise NIL.

Processing:

Errors signaled:

 None.

Notes:

 NULL is equivalent to NOT.

Examples:

 (NULL T)          ==> NIL
 (NULL NIL)        ==> T
 (NULL '(NIL))     ==> NIL
 (NULL (EQ 'A 'A)) ==> NIL
 (NULL (EQ 'A 'B)) ==> T

