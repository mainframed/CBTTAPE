
Function: NOT

Syntax:   (NOT arg1)

 arg1 (required,evaluated) - any LISP object.

Returns:

 T if arg1 is NIL, otherwise NIL.

Processing:

Errors signaled:

 None.

Notes:

 NOT is equivalent to NULL.

Examples:

 (NOT T)          ==> NIL
 (NOT NIL)        ==> T
 (NOT '(NIL))     ==> NIL
 (NOT (EQ 'A 'A)) ==> NIL
 (NOT (EQ 'A 'B)) ==> T

