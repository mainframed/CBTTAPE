PROC 0
 /* CLIST TO DECODE ADVENTURE SOURCE */
 /* ONCE IN PROGRAM SAY "NO" TO INTRUCTIONS, SAY "WIZARD", */
 /* SAY "SAUCE", AND THEN SAY "EXIT" */
 /* EDIT DATA SET NAMES AS APPROPRIATE */
ALLOC F(SOURCE) DA(SHR.DATA(PROGRAM))
ALLOC F(OBJECT) DA(SHR.DATA(DATABASE))
ALLOC F(STORAGE) DUMMY
CALL SHR(PROGRAM) '/?????@' /* WHERE ????? IS THE MAGIC PARM OF THE */
                            /* DAY DOCUMENTED ELSEWHERE */
