<<SCANLMEM>>
:
: SCANLMEM 'LIBNAME( MEMNAME)' SEARCHSTRING
:
:
PUSH
TRAP ON
DELETE
+IMPORT DSN=+A1+
+DELETEX  /+A2+/
IF (AWSCOUNT GT 0)
   RENUMBER
   RSHIFT 5
   +1 +A1+
   COPY @TEMP B
   IF (TC EQ 4)
      SAVE @TEMP
   ELSE
      UPDATE @TEMP
   ENDIF
ENDIF
TRAP OFF
POP
RETURN
