)ATTR  DEFAULT(%+_)
    @ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    ? TYPE(TEXT) INTENS(LOW) COLOR(GREEN)
)BODY EXPAND(\\)
@\-\ Numbering Defaults Entry Panel \-\
%&SID%===>_ZCMD                                                               +
%
+Enter the default numbering parameters below:
+        %===>_PARM#                                       %<=
+
+Suggest fromat:@1-6 FF 2 AP
+
+        where: @1-6+Represents from and to columns 1 to 6.
+               @FF +Represents using the previous line as the starting number.
+               @2  +Increment line numbers by 2.
+               @AP +Apply changes. Bypasses simulation mode.
+
+
+
+Use your%END+PF key to abandon processing.
)INIT
 &SID   = &ZSYSID
)PROC
)END
