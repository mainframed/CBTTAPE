/* REXX */
    Parse Upper Arg action
    If action = 'ON' Then Do
        ispclib = "'"$alkhlq()"."$alkllqr()"'"
                  ""
        "ALTLIB ACTIVATE APPLICATION(EXEC) DA("ispclib")"
    End
    Else "ALTLIB DEACTIVATE APPLICATION(EXEC)"

    Return rc
