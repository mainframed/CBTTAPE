Additional ISPF VER functions:
------------------------------

DSNAMEF   - Allow * and % in name.
DSNAMEFM  - Allow * and % in member name only.
DSNAMEPQ  - Adds trailing parentheses and quotes if missing.
DSNAMEQ   - Adds trailing quotes if missing.
FILEID    - Valid fileid in CMS syntax  (???)
LEN       - Length. (Eg. VER(&NAME,LEN,NG,8)
LIST      - Verify against a static list
LISTV     - Verify against a variable list (Eg. VER(&NAME,LISTV,&VARL)
LISTVX    - Exclude against a variable list (Eg. VER(&NAME,LISTV,&VARL)
LISTX     - Excluse against a static list
NAME      - Valid name syntax
NAMEF     - Valid name syntax allowing * and %
