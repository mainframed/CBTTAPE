* each company usually has a set of common ispf libraries where home-
* grown utilities are placed. put them here and rename this member to
* something similar to your company name, and update any drivers that
* call it to keep them in sync.
* note how we can use substitution variables in the dataset names.
ISPTLIB "'"SYSVAR("SYSUID")".ISPF.ISPPROF'"
