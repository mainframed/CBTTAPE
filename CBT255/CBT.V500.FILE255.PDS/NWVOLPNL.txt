)ATTR
  ! TYPE(OUTPUT)  INTENS(HIGH) SKIP(ON)
  + TYPE(TEXT)    INTENS(LOW) SKIP(ON)
  % TYPE(TEXT)    INTENS(HIGH) SKIP(ON)
  _ TYPE(INPUT)   INTENS(HIGH) CAPS(ON)
)BODY
%---------- BUILDING DEPOSIT NETWARE SYSTEMS LAN SERVER BACKUP SERVICES --------
%COMMAND ===>_ZCMD                                                     +
+
% SELECT LAN SERVER BACKUP RESTART MODE ===>_Z+
%   1.+ VOLUME SELECTION     %2.+ ALL SERVER VOLUMES
+
% SELECT LAN SERVER BACKUP TYPE FOR RESTART ===>_Z+
%   1.+ INCREMENTAL ( DAILY BACKUP )    %2.+ FULL ( WEEKLY BACKUP)
+
% SELECT LAN SERVER RESTART VOLUME(S): ( ENTER ANY CHARACTER TO SELECT VOLUME )
+   &VOL1 ===>_Z+   &VOL2 ===>_Z+   &VOL3 ===>_Z+   &VOL4 ===>_Z+
+
+   &VOL5 ===>_Z+   &VOL6 ===>_Z+   &VOL7 ===>_Z+   &VOL8 ===>_Z+
+
% NOTE: VOLUME SELECTION IS REQUIRED ONLY IF RESTART MODE 1 IS SPECIFIED.
+ ( ENTER KEY = SELECT/CONTINUE   ***   PF3 | PF15 = QUIT/CANCEL )
+
+
)INIT
  .ZVARS = '(LBU BUT VBU1 VBU2 VBU3 VBU4 VBU5 VBU6 VBU7 VBU8)'
 IF (&ZERRMSG ¬= ' ') .MSG = &ZERRMSG
)PROC
  VER (&LBU,NONBLANK)
  VER (&LBU,RANGE,1,2)
  VER (&BUT,NONBLANK)
  VER (&BUT,RANGE,1,2)
)END
