)ATTR
  ! TYPE(OUTPUT)  INTENS(HIGH) SKIP(ON)
  + TYPE(TEXT)    INTENS(LOW) SKIP(ON)
  % TYPE(TEXT)    INTENS(HIGH) SKIP(ON)
  _ TYPE(INPUT)   INTENS(HIGH) CAPS(ON)
)BODY
%------ BUILDING DEPOSIT NW LAN SERVER RESTORE PARMS VERIFICATION PANEL --------
%COMMAND ===>_ZCMD                                                     +
+
% * VERIFY THAT RESTORE PARAMETERS ARE CORRECT.
% * IF ALL RESTORE PARAMETERS ARE CORRECT, HIT ENTER TO PROCESS AND SUBMIT JOB.
% * IF NECESSARY, MAKE ANY REQUIRED CORRECTIONS, HIT ENTER TO PROCESS AND
%   SUBMIT JOB.
% * PF3 TO QUIT OR CANCEL RESTORE PROCESS.
+
+   LAN SERVER TO BE RESTORED ===>_SERVER +
+   LAN SERVER VOLUME TO BE RESTORED ===>_RESTVOL +
+   DIRECTORY PATH ===>_RESTDPN                                           +
+   LAN SERVER FILE TO BE RESTORED ===>_RESTFN      +
+   CONTAINER DATASET BACKUP DATE ===>_RESTDAT   +
+   INPUT CONTAINER DATASET:
+   ===>_RESTDSN                                     +
+   INPUT RESTORE TAPES:
+   ===>_TAPE1 + _TAPE2 + _TAPE3 + _TAPE4 + _TAPE5 + _TAPE6 + _TAPE7 + _TAPE8 +
+
+ ( ENTER KEY = PROCESS   ***   PF3 | PF15 = QUIT/CANCEL )
+
)INIT
 IF (&ZERRMSG ¬= ' ') .MSG = &ZERRMSG
)PROC
)END
