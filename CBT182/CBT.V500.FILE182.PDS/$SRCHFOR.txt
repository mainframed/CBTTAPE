SRCHFOR is a PDS interface to IBM's ISRSUPC Search-For via
the TSO/E Service Facility Routine, IKJEFTSR.

The global variable &SRCHOPT can be set in #PDSGENB/U to change the
IBM SuperC SEARCH-FOR process options.  The default is ANYC.
