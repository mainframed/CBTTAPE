)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢Function Prompt+-\-\-
%OPTION  ===>_ZCMD                                                             +
+
+ Extended Left/Right%===>+   + (Yes/No)
% Function prompt    %===>+???+ (Yes/No) Ordered Pending     %===>+   + (Yes/No)
+ Save in profile    %===>+   + (Yes/No)

%Function prompt:+
   Normally, the function selection and display panel is displayed when
   END is pressed and no line commands are pending.

   If only a single line command is pending, that function is chosen
   automatically and the function prompt panel is not displayed. If
   more than one line command is pending the action is dependent on
   other option settings.

   Respond with%YES+if you want¢PDS+to perform normal function prompting.

   Respond with%NO+if you want¢PDS+to display the ISPMODE panel after a
   END command and no line commands are pending. OR if already in the
   ISPMODE panel then exit PDS.
)END
