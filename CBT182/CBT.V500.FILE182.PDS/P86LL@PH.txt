)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢PDS o line command panel+-\-\-
%OPTION  ===>_ZCMD
%               ---------------------------------------------
%               |              PDS o line commands   %      |
                ---------------------------------------------
+
   The o line command option panel directs you as to which¢PDS+subcommands
   are available as line commands.  Enter one of the choices to proceed.

   Shorthand notes:
%   1.+ From the MEMLIST panel, you can bypass the line command
        option panel.  For example, enter%o.vp+as a line command
        to enter the VPSPRINT assistance panel.
%   2.+ For entries on the UTIL panel, their names may be used
        directly as line commands.  For a load module, enter:%jcl+
%   3.+ From the line command option panel, you can bypass the
        assistance panel.  For example, enter:%v.stats noload+
        to format the subcommand:%Verify memname stats noload+

   To get explanations of¢PDS+subcommands, you need to reference the
  ¢PDS+HELP data set.  Enter%H+from%OPTION ===>+to get assistance.

                                   (continued)
)PROC
 &ZCONT = P86PN@PO
)END
