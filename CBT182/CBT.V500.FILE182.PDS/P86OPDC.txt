)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
+-\-\-¢O.DCF   SCRIPT Subcommand+-\-\-
%OPTION  ===>¦ZCMD                                                             +
%
+Enter the member group name and any operands below for%DCF:
%===>¦LOPER
                                                                               +
+Operands: %memgroup   +  (e.g., *; start:end; start:; first*pat; range*; part/)
% BIND(obind {ebind})                  PAGE{({PROMPT}{{FROM} p {TO} q}
% CHARS(font1..font4)                        {{FROM} p FOR n}{{FROM} p ONLY})}
% CONTINUE/NOCONT                      PRINT{(copies,class,fcb,ucs)}
% CTF                                  PROFILE{(fileid)}/NOPROFILE
% DDUT/NODDUT                          QUIET
% DEST(station-id)                     SEARCH(libname)(opnum...)
% DEVICE(devtype)                      SEGLIB/NOSEGLIB
% FILE{(fileid)}                       SPELLCHK
% FONTLIB({filetype}{filemodel})       STOP
% FPASSES n                            SYON/SYOFF
% INDEX                                SYSVAR(n value...)
% LIB(libename...)(opnum...)           TERM
% MESSAGE({DELAY}{D}{TRACE})           TLIB
% NOSPIE                               TWOPASS
% NOWAIT                               UNFORMAT
% NUMBER                               UPCASE
% OPTIONS{(fileid)}                    @user-option
)INIT
  &LOPER  = '&OPERS'
  .CURSOR = LOPER
  .HELP   = P86OPHH
  &P86#DC1 = &PDS#DC1
  IF(&LOPER  = &Z) &LOPER=&P86#DC1
)PROC
 &P86#DC1=&LOPER
 &PDSZSEL='Dcf  &LOPER'
 &PDS#DC1 = &P86#DC1
 VPUT (PDS#DC1) PROFILE
)END
