)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    ¬ TYPE(TEXT) INTENS(HIGH) COLOR(RED)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢Response Required Panel+-\-\-
%OPTION  ===>_ZCMD                                                             +
+
   A¢PDS+subcommand requires a response to continue.

   If you wish to terminate the ISPMODE dialog, you may enter%RETURN+

   The%special control codes+perform the following special actions:
    %1+Suspend ISPF  % -+terminates ISPMODE; retains command log during session
    %F+Find          % -+positions the log to a different line
    %R+Recall        % -+Recall Change formats a DSNAME response for a failed
                         CHANGE command
    %X+Above/Below/All%-+clears the log relative to the current line


   Note:¢PDS+subcommands may%not+be entered on this panel; but the
   special control codes or any ISPF command except END may be entered
   on the command line.  If¢PDS+is prompting for a data set name with
   a¬PDS381A+message, END may be entered to terminate PDS.
)END
