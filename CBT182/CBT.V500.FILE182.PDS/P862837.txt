)Attr Default(`+_)
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_') color(turq)
   ! type(text) intens(high) caps(off) just(asis ) color(white)
   ` type(text) intens(high) caps(off) just(asis ) color(yellow)
   ~ type(text) intens(high) caps(off) just(asis ) color(green)
   # type(text) intens(high) color(blue) hilite(reverse)
   + type(text) intens(low ) color(turq)
     skip(on)
   ¬ area(SCRL) Extend(ON)
)Body Window(72,12)
¬help ----------------------------------------------------------------¬
)Area Help
+  Tom Conley gave a SHARE presentation in Baltimore
+  on the PDS Command Processor.  SHARE Session 2837 was named
+
+           #PDS - The Swiss Army Knife of Utilities+
+
+  His PDF slides from the session are in member $$S2837 on the
+  the distribution PDS in zip format.
+
+  FTP member $$S2837 to your PC in binary mode, unzip, and
+  launch the Adobe Acrobat Reader to view the slides.
)Init
&zwinttl = 'SHARE Session 2837 - August 2006'
&zup = P862837
)Proc
)End
