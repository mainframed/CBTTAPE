)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢o line command selection+-\-\-
%OPTION  ===>_ZCMD
%               ---------------------------------------------
                |              o line commands              |
                ---------------------------------------------
+
   The o line command option panel directs you as to which TCT
   commands are available as line commands; enter one of the listed
   options to proceed to the next panel or perform the function.

   Shorthand notes:
%   1.+ From the TCT panel, you can bypass the line command
        option panel.  For example, enter%o.ut+as a line command
        to enter the UTIL assistance panel.
%   2.+ From the TCT panel, you can bypass both panels for
        the UTIL option by entering the option name as a line command.
        For example, enter%dvol+to select the DVOL TSO command.
%   3.+ From the line command option panel, you can bypass the
        assistance panel.  For example, enter:%ut.dvol+

   To get explanations of¢PDS+subcommands, you need to reference the
  ¢PDS+HELP data set.  Enter%H+from the%OPTION ===>+line on the line
   command option panel reached from ISPMODE or MEMLIST.
)END
