)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢PDS LISTA Primary Commands+-\-\-
%OPTION  ===>_ZCMD                                                             +
+
   The following%special control codes+are supported as primary commands
   from the list allocation table display:
        %2+Clear table    %-+clear LISTA table display (same as X ALL)
        %7+Output table   %-+output the LISTA table to print or a data set
        %8+View Log       %-+display the log table
        %9+Swap panels    %-+switch to an alternate display
        %C+Change         %-+change to another data set
        %F+Find           %-+position to a LISTA table entry
        %O+Options        %-+display available options
        %X+Above/Below/All%-+clear the LISTA table relative to current position
       %LA+List allocation%-+refresh the allocation table
       %LF+List file      %-+display the file table
       %LV+List volume    %-+display the volume table
       %ML+Member list    %-+display the member table
      %CAX+List catalog   %-+display the active catalogs table
      %TCT+List command   %-+display the PDS/TSO command table
 %FUNCTION+Function       %-+function status and selection panel
 %    QUIT+Terminate      %-+terminate¢PDS+

)END
