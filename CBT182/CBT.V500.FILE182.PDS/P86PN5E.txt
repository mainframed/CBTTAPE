)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    ¬ TYPE(text) INTENS(HIGH) COLOR(RED) hilite(blink)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢PDS Subcommands+-\-\-
%OPTION  ===>_ZCMD                                                             +
+
  When¢PDS+is invoked for a partitioned data set, all subcommands are defined
  and all subcommand names are listed in response to an undefined name:
   ¬PDS771E NN IS AN INVALID SUBCOMMAND; ENTER "HELP" OR "OPTIONS" FOR HELP
+   SUBCOMMANDS:%ALIAS    AMBLIST  ATTRIB   BROWSE   CHANGE   COBANAL  COMPARE
                 COMPRESS CONTROL  COPY     DELETE   DIRENTRY DISPLAY  DSNAME
                 DSPRINT  EDIT     END      EXEC     FIND     FIXPDS   FSE
                 HELP     HISTORY  IF       ISPF     ISPMODE  LIST     LMA
                 LOAD     MAP      MATCH    MEMBERS  MEMLIST  OFFLOAD  OPTIONS
                 OUTCOPY  PATTERN  PRINTOFF RECALL   RENAME   REPLACE  REPRO
                 RESTORE  REVIEW   SMPGEN   SRCHFOR  SUBLIST  SUBMIT   TSO
                 TSOEDIT  TSOLIST  USAGE    VERIFY   VIEW     VPSPRINT XMIT
+ However, when¢PDS+is invoked for a non-partitioned data set, only the
  following subcommands are defined:
    SUBCOMMANDS:%BROWSE   CHANGE   COMPARE  CONTROL  DSNAME   DSPRINT  EDIT
                 END      EXEC     FIND     FIXPDS   FSE      HELP     ISPF
                 ISPMODE  LIST     OPTIONS  PRINTOFF RECALL   REPLACE  REVIEW
                 SUBMIT   TSO      TSOEDIT  TSOLIST  USAGE    VIEW     VERIFY
                 VPSPRINT
+ On the following pages, each of the¢PDS+subcommands is described briefly;
  note that the significant characters of each subcommand name is capitalized.
)PROC
  &ZCONT = P86PN5F
)END
