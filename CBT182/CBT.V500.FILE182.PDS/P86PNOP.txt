)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ¢ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ¬ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    ¦ TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-¢PDS CAX Primary Commands+-\-\-
%OPTION  ===>_ZCMD                                                             +
+
   The following%special control codes+are supported as primary commands
   from the active catalog display:
        %2+Clear table    %-+clear CAX table display (same as X ALL)
        %7+Output table   %-+output the CAX table to print or a data set
        %8+View Log       %-+display the log table
        %C+Change         %-+change to another data set
        %F+Find           %-+position to a CAX table entry
        %L+Locate         %-+locate xxxxx data in default sort column
        %O+Options        %-+display the available options
        %S+Sort           %-+sort the volume table
        %X+Above/Below/All%-+clear the CAX table relative to current position
       %LA+List allocation%-+display the list allocation table
       %LF+List file      %-+display the file table
       %LV+List volume    %-+prompt for list volume parameters
       %ML+Member list    %-+display the member table
      %TCT+List command   %-+display the PDS/TSO command table
 %FUNCTION+Function       %-+function status and selection panel
 %    QUIT+Terminate      %-+terminate¢PDS+
      %CAX+List catalog   %-+rebuild the CAX table from the current CAXWA chain
                            +after clearing table with X ALL
)END
