%TUTORIAL ------------  BACKUP SELECTION UTILITY   ---------------------TUTORIAL
%OPTION ===>_ZCMD                                                              +
+                       (CONTINUED FROM PREVIOUS PAGE)
+
% VOLUME+        OPTIONALLY SPECIFIES THE DISK VOLUME YOU ON WHICH TO
                 SEARCH FOR THE DATASET(S). DEFAULT IS ALL ONLINE VOLUMES
                 NOTE: THE%VOLUME+OPERAND CONFLICTS WITH THE
                      %VOLUME GROUP+OPERAND.

% VOLUME GROUP+  OPTIONALLY SPECIFIES A GROUP OF DISK VOLUMES ON WHICH
                 SEARCH FOR THE DATASET(S).
                 NOTE: THE%VOLUME+OPERAND CONFLICTS WITH THE
                      %VOLUME GROUP+OPERAND.
)END
