ISREDIT MACRO (HELP)
  IF &STR(&HELP) = ? THEN +
    DO
      ISPEXEC DISPLAY PANEL(T)
      EXIT CODE(12)
    END
     SET ZEDSMSG = &STR(&SYSTIME &SYSDATE)
     ISPEXEC SETMSG MSG(ISRZ001)
     EXIT CODE(0)
