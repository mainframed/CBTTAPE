/* REXX */
TRACE ERR
"ISPEXEC VGET (D1 D2 D3 D4 D5 LY) PROFILE"
IF LY = 50 THEN RETURN
IF LY = 0 THEN RETURN
IF D1 = D2 THEN
IF D2 = D3 THEN
IF D3 = D4 THEN
IF D4 = D5 THEN DO
  MS = 'YOU HAVE YAHTZEE.  TAKE THE 50 POINTS INSTEAD'
  "ISPEXEC VPUT (MS) PROFILE"
  RETURN 4
END
RETURN
