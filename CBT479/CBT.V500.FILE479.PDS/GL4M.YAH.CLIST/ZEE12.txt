/* REXX */
TRACE ERR
MS = ''
"ISPEXEC VPUT (MS) PROFILE"
"ISPEXEC VGET (LT D1 D2 D3 D4 D5 LY) PROFILE"
IF LY ¬= '' THEN DO
  MS = 'YOU HAVE ALREADY USED YOUR YAHTZEE'
  "ISPEXEC VPUT (MS) PROFILE"
  RETURN
END
LY = 0
IF D1 = D2 THEN
IF D2 = D3 THEN
IF D3 = D4 THEN
IF D4 = D5 THEN DO
   LY = 50
   MS = 'CONGRATULATIONS'
   "ISPEXEC VPUT (MS) PROFILE"
END
IF LY = 0 THEN DO
  MS = 'MAY AS WELL GIVE UP'
  "ISPEXEC VPUT (MS) PROFILE"
END
LT = LT + LY
"ISPEXEC VPUT (LY LT) PROFILE"
CALL ZEET
CALL ZEER
RETURN
