/* REXX */
CALL ZEEYC
IF RESULT = 4 THEN RETURN
TRACE ERR
MS = ''
"ISPEXEC VPUT (MS) PROFILE"
"ISPEXEC VGET (LT LC D1 D2 D3 D4 D5) PROFILE"
IF LC ¬= '' THEN DO
  MS = 'YOU HAVE ALREADY USED THE CHANCE SLOT'
  "ISPEXEC VPUT (MS) PROFILE"
  RETURN
END
TR = 1
LC = D1 + D2 + D3 + D4 + D5
LT = LT + LC
"ISPEXEC VPUT (LT LC TR) PROFILE"
CALL ZEELB
CALL ZEET
CALL ZEER
RETURN
