/* REXX */
TRACE ERR
LB = 0
LT = 0
"ISPEXEC VGET (D1 D2 D3 D4 D5 LB LT) PROFILE"
IF D1 = D2 THEN
IF D2 = D3 THEN
IF D3 = D4 THEN
IF D4 = D5 THEN DO
   LB = LB + 100
  IF LB > 300 THEN RETURN
  IF LB > 0 THEN DO
    LT = LT + 100
    MS = 'CONGRATULATIONS'
    "ISPEXEC VPUT (LB LT MS) PROFILE"
  END
END
RETURN
