)PANEL KEYLIST (ISRHELP ISR)
)ATTR
   % TYPE(ET)
   _ TYPE(NEF) PAD(USER) CAPS(ON)
   + TYPE(NT)
   # AREA(SCRL) EXTEND(ON)
)BODY
%TUTORIAL -------------------The Authority stream routine---------------TUTORIAL
%COMMAND ===>_ZCMD                                                             +
%
+  AUTHRUN                                                                     +
#S1                                                                            #
)AREA S1

   The authrun routine accepts two parameters, dataset name and
   target ACID. The dataset name must be a valid authority stream
   and each line starting at line 2 will be resolved to a TSO
   command. Each command is then issued and the return codes
   are accumulated into the final exiting return code.

)INIT
)PROC
)END
