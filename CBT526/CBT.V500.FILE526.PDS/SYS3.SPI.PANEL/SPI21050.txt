)PANEL KEYLIST (ISRHELP ISR)
)ATTR
   % TYPE(ET)
   _ TYPE(NEF) PAD(USER) CAPS(ON)
   + TYPE(NT)
   # AREA(SCRL) EXTEND(ON)
)BODY
%TUTORIAL -------------------The HIGHUID exec---------------------------TUTORIAL
%COMMAND ===>_ZCMD                                                             +
%
+  HIGHUID                                                                     +
#S1                                                                            #
)AREA S1

   This command accepts no parameters it returns the highest OMVS
   UID that is currently in use. This exec requires the BPXWIRAC
   exec that is delivered in the TSSOPMAT be installed in your
   system.  The ID invoking this command must also have the
   CONSOLE attribute in order to query the database for UID
   information.

)INIT
)PROC
)END
