)PANEL KEYLIST (ISRHELP ISR)
)ATTR
   % TYPE(ET)
   _ TYPE(NEF) PAD(USER) CAPS(ON)
   + TYPE(NT)
   # AREA(SCRL) EXTEND(ON)
)BODY
%TUTORIAL -------------------The output router exec---------------------TUTORIAL
%COMMAND ===>_ZCMD                                                             +
%
+  T50CMDO                                                                     +
#S1                                                                            #
)AREA S1

   This command accepts one parameter, the output dataset.  It
   does two VGETs one for the variable ARG which must be the
   command to be issued, and oine for the variable DISP which
   must be a valid allocation disposition ( MOD SHR OLD etc.) .
   It the issues the command and deposits the output in the
   dataset specified.

)INIT
)PROC
)END
