)PANEL KEYLIST (ISRHELP ISR)
)ATTR default(%+@)
   % TYPE(ET)
   @ TYPE(NEF) PAD(USER) CAPS(ON)
   + TYPE(NT)
   # AREA(SCRL) EXTEND(ON)
)BODY
%TUTORIAL -------------------The variable update panel------------------TUTORIAL
%COMMAND ===>@ZCMD                                                             +
%
+  Tssdpanl                                                                    +
#S1                                                                            #
)AREA S1

   This command displays a screen that allows you to update variables
   in the current profile pool. The variables must be passed in a
   period delimited string an example of which is below.

   tssdpanl JCARD.RECFILE.ACCTDSN.DSBACK.MSCA.TSOUNIT.TSOSIZ

   Note that in the command member the string would be prefixed
   with an underscore as seen below:

   _JCARD.RECFILE.ACCTDSN.DSBACK.MSCA.TSOUNIT.TSOSIZ


)INIT
)PROC
)END
