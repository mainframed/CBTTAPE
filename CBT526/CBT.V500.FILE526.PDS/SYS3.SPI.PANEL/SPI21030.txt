)PANEL KEYLIST (ISRHELP ISR)
)ATTR
   % TYPE(ET)
   _ TYPE(NEF) PAD(USER) CAPS(ON)
   + TYPE(NT)
   # AREA(SCRL) EXTEND(ON)
)BODY
%TUTORIAL -------------------The Output trapper exec--------------------TUTORIAL
%COMMAND ===>_ZCMD                                                             +
%
+  FSDISP                                                                      +
#S1                                                                            #
)AREA S1

   This command accepts one parameter the command to be issued.
   The command is then issued and the output trapped and inserted
   into a temporary dataset which is then browsed.

)INIT
)PROC
)END
