To install the $VSAMIDX exec, do the following:

   o   Copy $VSIDX01 to the system ISPF panel data set.
   o   Copy $VSAMIDX to the system CLIST data set.

Syntax for $VSAMIDX:

       $VSAMIDX VSAMdsname

