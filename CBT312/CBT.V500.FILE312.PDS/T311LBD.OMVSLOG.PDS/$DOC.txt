The OMVSLOG ISPF Dialog provides a generalized interface to display both the
active log file and the archived (to z/OS datasets) log files for an omvs
application.

See the exec for details on customization (see *custom*).

Members of this PDS are:

    $DOC    - what you are reading
    EXEC    - TSO Transmit of the REXX Library
    PANELS  - TSO Transmit of the ISPF Panel Library
    $RECV   - TSO Command to RECEIVE EXEC and PANELS into
              usable datasets

Dependencies:
  - This ISPF dialog utilizes the STEMVIEW utility which can be found
    on the CBTTape FILE 183.
