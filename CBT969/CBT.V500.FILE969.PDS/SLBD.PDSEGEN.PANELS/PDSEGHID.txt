)attr default(%+_)
~ type(text) intens(high) color(&clrh) hilite(&clrhr)
_ type( input) intens(low ) just(left ) hilite(uscore) color(&clrw )
` type(text) intens(low ) caps(off) just(asis ) color(&clrt)
+ type(text) intens(high) caps(off) just(asis ) color(&clrt)
# type(text) intens(high) caps(off) just(asis ) color(&clrt)
% type(text) intens(high) color(&clrw)
@ area(SCRL) Extend(ON)
)Body Expand(\\)
%Tutorial -\-\(~PDSE V2 Member Generations%)\-\- Tutorial
%Command ===>_ZCMD                                                             +
%
@help ------------------------------------------------------------------------@
)Area Help
%  ID+and%MINE
`
` %ID`followed by 1 to 8 characters will be used to filter the displayed
`  member list to only those members where the provided characters are found
`  in the userid.
`
`  Syntax:%ID xxxx
`
` %MINE`will display only those members that have a userid the same as the
`  active userid.
`
`  Syntax:%MINE
`
`  Note: These act as toggles to filter or not to filter.
`
)Init
 .cursor = &zcmd
)PROC
 &ZUP = PDSEGH0
)END
/* Copyright (c) 2018 by Lionel B. Dyck */
/* GNU GENERAL PUBLIC LICENSE V3        */
