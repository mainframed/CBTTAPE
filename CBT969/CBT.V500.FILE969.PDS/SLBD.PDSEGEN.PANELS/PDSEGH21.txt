)attr default(%+_)
~ type(text) intens(high) color(&clrh) hilite(&clrhr)
_ type( input) intens(low ) just(left ) hilite(uscore) color(&clrw )
` type(text) intens(low ) caps(off) just(asis ) color(&clrt)
+ type(text) intens(high) caps(off) just(asis ) color(&clrt)
# type(text) intens(high) caps(off) just(asis ) color(&clrt)
% type(text) intens(high) color(&clrw)
)Body Expand(\\)
%Tutorial -\-\(~PDSE V2 Member Generations%)\-\- Tutorial
%Command ===>_ZCMD                                                             +
%
%  SETMACRO (SM)
`
`  The SetMacro command enables the definition and management of ISPF Edit
`  Initial Edit macros. These are defined based on the dataset suffix and
`  will only be invoked if the defined suffix matches the active dataset.
`
`  The definition includes:
`
`      Dataset Suffix
`      Edit Macro Name
`      Edit Macro Parm (optional)
`
`  Syntax:  SETMacro or SM
`
)Init
 .cursor = &zcmd
)PROC
 &ZUP = PDSEGH0
)END
/* Copyright (c) 2207 by Lionel B. Dyck */
/* GNU GENERAL PUBLIC LICENSE V3        */
