)ATTR
 /*  (C) COPYRIGHT AMDAHL CORPORATION  1995. All Rights Reserved    */
 ! TYPE(TEXT) INTENS(LOW) COLOR(YELLOW)
 # TYPE(TEXT) INTENS(HIGH) COLOR(TURQ)
 $ TYPE(OUTPUT) COLOR(RED)
 ? TYPE(INPUT) PAD(_) CAPS(ON)
)BODY WINDOW(76,14) ASIS
#                  COPY / NOCOPY HELP
!
!  #COPY/NOCPY!specifies whether or not the 3990 is to copy the primary
!   volume onto the secondary volume when establishing a duplex pair.
!   This is used only with the SETSECONDARY parameter.
!  #COPY! specifies that the 3990 is to copy the primary volume onto the
!   secondary when establishing a duplex pair.
!  #NOCOPY!specifies that the primary and secondary devices are
!  #identical!and the 3990 does not need to copy the primary volume onto
!   the secondary to establish the duplex pair. You should only specify
!   the NOCOPY parameter when both devices have been initialized with
!   ICKDSF using the same initialization parameters and contain no
!   application data.
)END
