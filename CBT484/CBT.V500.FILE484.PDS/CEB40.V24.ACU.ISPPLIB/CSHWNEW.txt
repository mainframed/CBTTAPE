)ATTR
 /*  (C) COPYRIGHT AMDAHL CORPORATION  1995. All Rights Reserved    */
)BODY EXPAND(\\)
+\-\%Amdahl Cache Utility What's New\-\
%COMMAND ===>_ZCMD                                                             +
+                                                                              +
   This panel can also be displayed by typing %NEW+ in the command area
   of the controller or device panel.

   %V2.4+included the following changes and new functions:

   Corrected logic for 4 digit device numbers on selection screen.
   Device model information is now extracted from DCE portion of UCB.
   Terminate logging if B37 abend occurs on log data set.
   Displays can be sorted by various fields.

   New commands in V2.4:
  %SORT+    Change the order of the displayed data.
   New commands in V2.3:
  %DCACHE+  Display status of all devices on cache controllers in a short form.
  %EXCEPT+  Display all controllers that have existing abnormal conditions.
  %MEMBER+  Display a panel for modifying an existing selection group member.
  %VDATE+   Display version, change date, userid, and use count.

)END
