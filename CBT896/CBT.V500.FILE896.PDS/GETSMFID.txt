 /* REXX GETSMFID - Displays the current system's SMFID              */
  numeric digits 10
  cvt = c2d(storage('10',4))
  cvtsmca = c2d(storage(d2x(cvt+x2d('c4')),4))
  smcasid = storage(d2x(cvtsmca+x2d('10')),4)
  say "Currnet SMF ID:" smcasid
