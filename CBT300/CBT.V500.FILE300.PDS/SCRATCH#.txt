)F FUNCTION -
  THE SCRATCH COMMAND SCRATCHES A DATASET OR LIST OF DATASETS
  FROM A DESIGNATED VOLUME WITHOUT AFFECTING OR USING THE CATALOG.
)X SYNTAX -
         SCRATCH   'DSNAME' / ('DSNAME' ...)
                   VOLUME (VOLID)
                   UNITNAME ('UNITNAME') / DEVTYPE ('HEX DIGITS')
                   PURGE
  REQUIRED - DSNAME(S), VOLUME, AND UNITNAME OR DEVTYPE
  DEFAULTS - NONE
)O OPERANDS -
))DSNAME   - NAME(S) OF DATASET(S) TO BE SCRATCHED.

))VOLUME('VOLID') - ID OF VOLUME ON WHICH THE DATASET(S) RESIDE.

))UNITNAME('UNITNAME') - GENERIC OR USER-DEFINED NAME OF UNIT
             ON WHICH DATASET(S) RESIDE.

))DEVTYPE('HEX DIGITS') - DEVICE TYPE (UCBTYP FIELD) OF UNIT
             ON WHICH DATASET(S) RESIDE.

))PURGE                 - CAUSES AN UNEXPIRED DATE TO BE
             OVERRIDEN, SCRATCHING THE DATASET.
