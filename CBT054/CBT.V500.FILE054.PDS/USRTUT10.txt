)ATTR DEFAULT(%+_)
/* % TYPE(TEXT  ) INTENS(HIGH)              DEFAULTS DISPLAYED FOR */
/* + TYPE(TEXT  ) INTENS(LOW )                   INFORMATION ONLY  */
/* _ TYPE( INPUT) INTENS(HIGH) CAPS(ON ) JUST(LEFT )               */
)BODY
%--------------   DELUXE User List RACF Registration Tutorial   ---------------
%COMMAND ===>_ZCMD
+
  %COMMAND:+     Acceptable entries are all ISPF primary commands
                 (END, RETURN, SWAP, etc.).
+
  %Other:+       Enter information to be used to update a user's
                 RACF profile.

   Press%END+to return.

)INIT
)PROC
)END
