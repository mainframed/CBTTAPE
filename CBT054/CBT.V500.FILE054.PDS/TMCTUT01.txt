)ATTR DEFAULT(%+_)
/* % TYPE(TEXT  ) INTENS(HIGH)              DEFAULTS DISPLAYED FOR */
/* + TYPE(TEXT  ) INTENS(LOW )                   INFORMATION ONLY  */
/* _ TYPE( INPUT) INTENS(HIGH) CAPS(ON ) JUST(LEFT )               */
)BODY
%-----------------------   DELUXE TMC Display Tutorial   -----------------------
%COMMAND ===>_ZCMD
+
  %COMMAND:+     Acceptable entries are all ISPF primary commands
                 (END, RETURN, UP, DOWN, SWAP, etc.).
                 It is also possible to enter LOCATE commands for
                 locating specific data set names
                 (L SLS, LOC SLS.SLS002, LOCATE REF, etc.).
+
  %SCROLL:+      Acceptable entries are any scroll amounts which are
                 valid under ISPF (PAGE, CSR, HALF, etc.).
+
  %Sel:+         Acceptable entries for this line command are:
                    %S+ Select chosen tape data set for scratch processing.
                    %R+ Remove chosen tape data set from scratch processing.

   Press%END+to return.

)INIT
)PROC
)END
