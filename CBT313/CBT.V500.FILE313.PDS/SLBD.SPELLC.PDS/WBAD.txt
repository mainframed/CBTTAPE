This is a test record

Ths mmber has a few missspelled words so tha this member can
be used to verify that the spelling verification actually will find
invalid words using the spellc (which is an invalid word) command.

Ths MMBER has a few MISSSPELLED words SO THA this member Can
be Used TO verify that the spelling verification actually will find
invalid words using the SPELLC (which is an invalid word) command
using the INGORE CAPS option both Yes and No.

Here is An$other example that show words with special characters are
ignored.
